#lesson 1
created dataframe and series
Learned to Read and Write csv files
CSV Means comma seperated values

#Lesson 2
Learned about Indexing the csv datas
seperating datas from whole datas by using command like loc, iloc and isin

#Lesson 3
here first method i learned is about describe() this usefull to finde details about numerical and sring values. for number values it provide men value, count maximum vaue etc and for string values it provide count, unique name that come again and again and give the frequeny of the top name.
mean() function used for find the average of datas in a dataframe or series.
unique() function used for find each and every names that are uniquly identified in a dataframe.
value_count() function used for find the count of each values
map() function transform the data inside the datafram but not change the actual datas and give the output as series. mapes just define one instance. from that pattern remaining points are edited. 
apply() function also not change the original one but give transformed output as a Dataframe. apply() need a method that should defined in earler.
